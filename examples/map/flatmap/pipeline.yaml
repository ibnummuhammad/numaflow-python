apiVersion: numaflow.numaproj.io/v1alpha1
kind: Pipeline
metadata:
  name: simple-pipeline
spec:
  limits:
    readBatchSize: 2
  vertices:
    - name: kafka-in
      source:
        kafka:
          brokers:
            - host.minikube.internal:29092
          topic: coba
          consumerGroup: console-consumer-78922
    - name: flatmap
      udf:
        container:
          image: "ibnummuhammad/map-flatmap:v5"
          env:
            - name: PYTHONDEBUG
              value: "true"
      containerTemplate:
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: "500m"
            memory: 1Gi
        env:
          - name: NUMAFLOW_DEBUG
            value: "true" # DO NOT forget the double quotes!!!
    - name: out
      sink:
        # A simple log printing sink
        log: {}
  edges:
    - from: kafka-in
      to: flatmap
    - from: flatmap
      to: out
